package com.ff.challenge.feature.auth.data.local

object Constants {
    const val DATABASE_VERSION = 1
    const val DATABASE_NAME = "ff.auth"
    const val DATABASE_ASSETS = "databases/$DATABASE_NAME.db"
    const val TABLE_USERS = "users"
}